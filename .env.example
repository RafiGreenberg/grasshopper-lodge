# Example environment variables for the booking API
# If you don't provide SMTP settings the server will still store bookings to data/bookings.json

# SMTP server settings (for sending notification emails)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user@example.com
SMTP_PASS=your-smtp-password

# Where to send booking notifications (defaults to SMTP_USER if not set)
NOTIFY_EMAIL=owner@example.com

# From address for outgoing emails
FROM_EMAIL="Grasshopper Lodge <no-reply@grasshopperlodge.com>"

# Optional subject override
EMAIL_SUBJECT="New booking request — Grasshopper Lodge"
